# Standards Crosswalk – CYBR 698 Capstone (Homework 12)

| **Scope Element** | **CSF 2.0 Outcome (Exact Text + Page)** | **ZTA Tenet (Exact Text + Page)** | **ISO 27001 Theme** | **Evidence Pointer** | **Rationale (100–150 words)** |
|-------------------|------------------------------------------|------------------------------------|----------------------|------------------------|--------------------------------|
| **Containerized reproducible workflow (Dockerfile + analysis script)** | **PR.PS-01: Configuration management practices are established and applied.** (CSF 2.0, p. 21) [3] | **“All communication is secured regardless of network location.”** (Zero Trust Tenet 2, p. 7) [4] | Technological | `Dockerfile`, `requirements.txt`, `src/run.py`, `README.md` | The containerized workflow defines the execution environment through explicit configuration in the Dockerfile and dependency file. This satisfies PR.PS-01 because it ensures configuration consistency, prevents drift, and applies configuration management practices in a structured, repeatable way [3]. It also aligns with Zero Trust Tenet 2, which states that communication should be secured regardless of location [4]. By packaging the analysis inside a controlled container rather than depending on local system trust, the workflow supports isolated execution, predictable performance, and a clear baseline for future hardening. |
| **One-step analysis script generating metrics and logs** | **ID.RA-01: Vulnerabilities in assets are identified, validated, and recorded.** (CSF 2.0, p. 19) [3] | **“The enterprise monitors and measures the integrity and security posture of all owned and associated assets.”** (Zero Trust Tenet 5, p. 7) [4] | Technological | `src/run.py`, `data/`, `docs/metric.txt`, `docs/log.txt` | The analysis script loads sample data, performs a computation, and produces outputs that are recorded in the `docs` directory. This aligns with ID.RA-01 because the workflow identifies, calculates, and records results in a structured way that mirrors how security-relevant findings are validated in real environments [3]. The script also generates logs and artifacts that enable monitoring of the system’s behavior, reflecting Zero Trust Tenet 5 by supporting measurement of asset posture over time [4]. This design promotes transparency, provides evidence of execution, and supports future enhancements such as anomaly detection or automated validation. |
| **Logged outputs stored in `/docs` (metric, log, chart)** | **PR.PS-04: Log records are generated and made available for continuous monitoring.** (CSF 2.0, p. 21) [3] | **“The enterprise monitors and measures the integrity and security posture of all owned and associated assets.”** (Zero Trust Tenet 5, p. 7) [4] | Organizational / Technological | `docs/metric.txt`, `docs/log.txt`, `docs/chart.png` | The `/docs` directory stores all artifacts generated by the analysis, including metrics, logs, and charts. This supports PR.PS-04 because each execution produces log records that are preserved in a clearly defined location suitable for monitoring [3]. These files also support Zero Trust Tenet 5 because they provide measurable evidence about how the analysis behaves, enabling users to verify integrity, detect deviations, and maintain accountability [4]. Version-controlling these outputs reinforces good governance practices and creates a strong foundation for repeatable, evidence-based evaluation as the capstone project matures. |

---

## **Profile Snippet (Current → Target Profile)**  
*(Following terminology from NIST SP 1301)*

**Current Profile:**  
CSF Outcomes PR.PS-01, PR.PS-04, and ID.RA-01 are implemented at a basic but functional level. The container defines configuration, logs are consistently generated, and the analysis script runs reliably, but processes for review and monitoring are not yet formalized.

**Target Profile:**  
By the end of the capstone, these same Outcomes will be implemented more formally. Configuration changes for PR.PS-01 will be reviewed regularly, logs for PR.PS-04 will be checked for anomalies, and analysis results for ID.RA-01 will be evaluated using clearer criteria. This represents movement from a basic Current Profile to a more mature Target Profile as described in NIST SP 1301 [5].

